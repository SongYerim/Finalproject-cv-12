fastapi>=0.110.0              # Web Framework (최신 기능 및 보안 패치 포함)
uvicorn[standard]>=0.29.0     # ASGI Server (비동기 서버 실행용)
python-dotenv>=1.0.1          # .env 파일 로드용 (1.0.0 이상 권장)
httpx>=0.27.0                 # Async HTTP Client (네이버/TMAP API 비동기 호출용)
pydantic-settings>=2.2.0      # Environment Management (FastAPI 설정 관리)
requests>=2.31.0              # Sync HTTP Client (동기 호출이 필요할 경우 대비)